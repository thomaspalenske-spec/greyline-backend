from app.services.risk_engine import RiskEngine
from app.services.portfolio_engine import PortfolioEnginefrom app.services.risk_engine import RiskEngine
from app.services.portfolio_engine import PortfolioEnginerouter = APIRouter()risk_engine = RiskEngine()
portfolio_engine = PortfolioEngine()from fastapi import APIRouter

router = APIRouter()


@router.get("/governance/status")
def governance_status():
    return {
        "system": "GreyLine",
        "status": "ACTIVE",
        "risk_state": "NORMAL",
        "survivability_status": "STRONG",
        "deployment_bias": "SELECTIVE_AGGRESSION",
        "confidence": "SIMULATED"
    }

