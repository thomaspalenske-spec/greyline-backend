# Runtime dependencies, pinned to the versions the working venv actually runs.
# The file previously listed only fastapi + uvicorn, so a fresh venv on a new host
# would fail at import: the app also needs python-dotenv (main.py load_dotenv),
# requests (TradeStation + Unusual Whales HTTP), and Jinja2 (dashboard templates).
# pydantic/starlette come in via fastapi but are pinned so a rebuild is reproducible.
fastapi==0.128.8
uvicorn[standard]==0.39.0
python-dotenv==1.2.1
requests==2.32.5
Jinja2==3.1.6
pydantic==2.13.4

# Dev / test only (not needed to run the service):
#   pytest==8.4.2
